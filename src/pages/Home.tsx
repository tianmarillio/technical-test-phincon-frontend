import ExampleComponent from '@/components/ExampleComponent';

const Home = () => {
  return (
    <main className="container mx-auto grid h-screen place-content-center">
      <ExampleComponent />
    </main>
  );
};

export default Home;
